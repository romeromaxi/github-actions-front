export enum OffererTypes {
  SA = 1,
  SRL = 2,
}

export enum OffererUserRoleTypes {
  ResponsibleAFIP = 1,
  Administrator = 2,
  Operator = 3,
  Reading = 4,
}

export enum OffererStatus {
  Active = 1,
  Analisys = 2,
  Inactive = 3,
}
