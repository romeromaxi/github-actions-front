import { makeStyles } from '@mui/styles';
import { useMediaQuery } from '@mui/system';

export default makeStyles({
  snackbarAlert: {
    marginBottom: '5px',
    width: 'auto',
    maxWidth: '500px',
    alignItems: 'center',
    padding: '16px'
  },
});
