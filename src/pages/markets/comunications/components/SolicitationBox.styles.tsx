import { grey } from '@mui/material/colors';
import { SxProps } from '@mui/material';

export const solicitationBoxSx: SxProps = {
  borderRadius: '0.475rem',
  border: `1px dashed ${grey[300]}`,
  padding: '1.2rem ',
};
